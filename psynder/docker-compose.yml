version: '3.1'
services:
  server:
    build: .
    depends_on:
      - db
    ports:
      - "443:443"
      - "444:444"
    volumes:
      - ./keys:/keys
      - ./config-docker.yml:/config.yml
      - ./config-docker-shelters.yml:/config_shelters.yml
  db:
    image: postgres:14.0-alpine3.14
    environment:
      POSTGRES_PASSWORD: 12345678
    volumes:
      - ./initdb.sql:/docker-entrypoint-initdb.d/initdb.sql
